# 开发日志（Append-only）

> 可以记录进展、问题、结论；但“决策”仍以 `决策日志.md` 为准。

## 记录格式（建议）
### YYYY-MM-DD
- 今日目标：
- 实际完成：
- 遇到的问题：
- ✅定稿结论（可写入决策日志/任务单）：
- 明日计划：

---

### 2026-02-09
- 今日目标：建立文档与日志体系，让后续讨论可转 Codex 工单。
- 实际完成：确定最小文档集合与 Append-only 记录规则。
- ✅定稿结论：采用“✅定稿才入决策/任务单”的机制。
- 明日计划：输出第一批 Codex 工单：工程骨架 + RBAC + 基础库表。

### 2026-02-10 14:14:05
- 目标：初始化根目录 Git 仓库并准备 GitHub 首版上传。
- 执行：创建根目录 .gitignore（忽略 .idea、target、开题报告 docx）；开始执行 git 初始化与首提交流程。
- 结果：已完成忽略规则落地，进入 git init/add/commit 阶段。
- 下一步：初始化 main 分支并完成首提交；拿到远端 URL 后推送并回写结果。

### 2026-02-10 14:14:54
- 目标：完成首提交并准备推送 GitHub。
- 执行：git init -b main、git add .、忽略规则核验、尝试 git commit。
- 结果：commit 被阻塞（未配置 user.name / user.email）；其余步骤已完成。
- 下一步：设置本仓库提交身份后重试 commit，并绑定 origin 推送 main。

### 2026-02-10 14:24:10
- 目标：完成 GitHub 首版上传并验证验收项。
- 执行：设置仓库提交身份；完成首提交（b921f2f）；绑定 origin=https://github.com/BCHIIEONG/jnu.git；推送 main；执行状态/忽略/远端分支检查。
- 结果：首版上传成功，origin/main 已创建并跟踪；开题报告 docx 命中忽略规则且未纳入版本库。
- 下一步：按任务单进入 BOOT-001 开发；后续每次操作继续仅追加写入本日志。

### 2026-02-10 14:42:55
- 目标：清理 GitHub 中仅供 Codex 使用的说明文件。
- 执行：删除 4 个 Codex 专用文件；更新 .gitignore 增加 Codex 文件忽略规则。
- 结果：仓库已完成清理，待提交并推送到 origin/main。
- 下一步：完成 commit/push，并给出明日向老师汇报的进展与下一步开发节奏。

### 2026-02-10 14:52:56
- 目标：纠正“仅远端删除 Codex 文件”的执行偏差，并保留本地可回退备份。
- 执行：从历史提交恢复 4 个 Codex 文件到本地工作区；在 备份/2026-02-10_codex_local_only 下生成同内容副本；更新 .gitignore 忽略 备份/。
- 结果：GitHub 仍不包含 Codex 文件；本地与备份均可回退使用；后续备份目录不会再被上传。
- 下一步：进入 BOOT-001 开发，并持续按“开发日志追加”记录。

### 2026-02-10 14:57:42
- 目标：把用户新确认的长期约束固化到执行说明，防止后续遗忘。
- 执行：更新 docs/CODEX_执行说明.md，新增“用户长期约束（2026-02-10）”；同步修正定稿记录路径为 log记录/定稿记录.txt。
- 结果：本地执行说明已固化规则；后续将按“新增规则即追加写入本文件”执行。
- 下一步：继续按 BOOT-001 开发节奏推进，并保持开发日志追加记录。

### 2026-02-10 15:06:04
- 目标：完成 BOOT-001 工程底座并形成可汇报成果。
- 执行：实现统一返回/全局异常/traceId 过滤器/参数校验统一返回；新增 /api/ping 与 /api/ping/echo；配置 Swagger 与 Actuator health；补充集成测试与文档状态更新。
- 结果：执行 lab-flow-report-system\\.\\mvnw.cmd -U clean test 通过（BUILD SUCCESS，5 tests）。
- 下一步：进入 DB-001（Flyway + RBAC 最小表结构）与 AUTH-001（JWT/RBAC 骨架）。

### 2026-02-10 16:29:15
- 目标：按 M2 追赶计划完成 DB-001、AUTH-001 与报告主链后端闭环。
- 执行：新增 Flyway 迁移脚本（V1~V3）、MyBatis-Plus 持久层、JWT 鉴权与 RBAC、安全统一 401/403 返回；实现任务/提交/批阅/反馈/导出接口；补充 H2 测试配置与端到端集成测试。
- 结果：lab-flow-report-system\\.\\mvnw.cmd -U clean test 通过（Tests run: 8, Failures: 0, Errors: 0）；主链接口可联调演示。
- 下一步：如需前端演示，进入 Vue 最小页面联调；如继续后端，进入签到链与设备链。

### 2026-02-10 19:26:38
- 目标：修复 XAMPP MariaDB(MySQL 5.5) 下 Flyway V2 迁移失败导致后端无法启动的问题。
- 执行：将 V2__task_report_core.sql 中的 CLOB 字段类型改为 LONGTEXT（兼容 MariaDB/MySQL）；回归执行 mvn test。
- 结果：lab-flow-report-system\\.\\mvnw.cmd -U clean test 通过（BUILD SUCCESS）。
- 下一步：删除并重建本机库 lab_flow_report（清理失败迁移记录），再启动后端验证 Swagger 可访问。

### 2026-02-15 16:21:48
- 目标：建立“进度报告”材料归档目录，后续所有图/Word/PPT 统一存放，便于向老师汇报。
- 执行：在 C:\\Users\\benso\\Downloads\\进度报告 下创建 01_证据截图/02_用例图/03_数据库设计/04_PPT/05_演示脚本；新增 00_说明_命名与归档规则.txt 记录命名规范。
- 结果：归档目录结构已固定，可直接开始把截图、用例图、数据库说明、PPT、演示脚本按规则落盘。
- 下一步：按“CSV 中文乱码修复 -> 用例图与表说明产出”的顺序补齐汇报材料，并持续追加写入本日志。

### 2026-02-15 16:25:42
- 目标：让“进度报告”目录不只是空文件夹，提供可直接使用的模板与证据文件。
- 执行：在各子目录新增 README_*.txt 说明“放什么/怎么命名”；生成 03_数据库设计\\2026-02-15_db_tables.txt（SHOW TABLES 输出）；新增 05_演示脚本\\演示_主链路_保存CSV.ps1（跑通主链并把 CSV 保存到 01_证据截图）。
- 结果：进度报告目录已包含可用模板与数据库表清单，后续只需要补截图与用例图即可形成汇报材料包。
- 下一步：运行演示脚本生成一份“comment 中文不为 ????”的 CSV；再做 Excel 兼容（BOM）以彻底解决中文乱码。

### 2026-02-15 16:35:45
- 目标：生成可直接用于向老师汇报的“用例图 PNG + 数据库设计 Word（含表格字段解释）”材料。
- 执行：在 Downloads\\进度报告\\02_用例图 生成 5 张用例图 PNG（登录鉴权/教师发布任务/学生提交报告/教师批阅评分/导出成绩CSV）；在 Downloads\\进度报告\\03_数据库设计 生成 ER 图 PNG 与《数据库设计说明_v1.docx》（按每张表生成字段解释表格、外键与索引说明）。
- 结果：材料已落盘，可直接打开查看并用于汇报。
- 下一步：补齐 01_证据截图（health、swagger、链路调用、CSV 文件），以及针对 CSV 中文乱码做“数据源修复 + Excel(BOM)兼容”。

### 2026-02-15 16:58:11
- 目标：对当前项目状态进行总结，并输出一份“从项目到细功能+权限边界+里程碑”的完整规划报告供审查。
- 执行：根据 log记录/定稿记录.txt（范围与周计划）、PLANS.md（当前状态）、docs/（接口/部署/测试等）生成 Word 报告《项目完整规划报告_v1.docx》，输出到 Downloads\\进度报告 根目录。
- 结果：C:\\Users\\benso\\Downloads\\进度报告\\2026-02-15_项目完整规划报告_v1.docx 已生成，可直接用于老师审查与后续对照执行。
- 下一步：按报告中“模块分解与时间计划”补齐未实现模块（签到/设备/进度/查重/管理端）并同步证据截图到 01_证据截图。

### 2026-02-15 17:08:06
- 目标：解决 CSV 导出在 Excel 下常见的 UTF-8 识别问题，并提升 Swagger 联调体验（可直接输入 JWT）。
- 执行：导出接口改为返回 UTF-8 字节并在开头追加 BOM（EF BB BF）；OpenAPI 增加 Bearer JWT 安全方案以显示 Swagger 的 Authorize；更新集成测试覆盖 BOM。
- 结果：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过（BUILD SUCCESS，8 tests）。
- 下一步：用教师 token 实测导出 CSV 在 Excel 双击打开是否中文正常；若仍有“评语变 ????”，优先修复评语写入端（PowerShell/客户端编码）再验证导出。

### 2026-02-15 17:13:17
- 目标：提供“最终验收”逐步测试脚本，并先由 Codex 本机执行一遍验证无问题。
- 执行：新增进度报告脚本 `C:\\Users\\benso\\Downloads\\进度报告\\05_演示脚本\\测试_全套检查_逐步执行.ps1`（可选先跑 mvn test，再跑 E2E 主链路并保存 CSV，校验 UTF-8 BOM）；修正脚本对 Windows PowerShell 5.1 的 UTF-8 JSON 解码兼容（按字节流 UTF-8 解码后 ConvertFrom-Json）。
- 结果：本机执行 `测试_全套检查_逐步执行.ps1 -SkipMvn` 通过；已生成证据文件 `C:\\Users\\benso\\Downloads\\进度报告\\01_证据截图\\2026-02-15_csv_task-3.csv`，其中中文评语正常且 CSV 含 BOM。
- 下一步：你在最终验收时执行同一脚本（建议不加 -SkipMvn），并补齐 health/swagger/PowerShell 截图到 01_证据截图。

### 2026-02-23 20:56:29
- 目标：补齐“已汇报的登录+页面”真实实现，落地 FE-001 三端最小可演示前端，并完成 CORS-001 以支持浏览器联调与下载。
- 执行：
  - 后端：新增 `CorsConfig` 允许 `http://localhost:5173` 跨域访问，暴露 `X-Trace-Id`、`Content-Disposition`；保持 Security `.cors()` 生效。
  - 前端：新增 `frontend/`（Vite + Vue3 + TS + ElementPlus + Pinia + Router），实现登录页与学生/教师/管理员 3 个最小页面，并提供管理员 RBAC 自检面板；教师端支持导出成绩 CSV 下载。
  - 配置：根目录 `.gitignore` 忽略 `frontend/node_modules/`、`frontend/dist/`、`frontend/.env*`。
- 结果：
  - 后端：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过（BUILD SUCCESS，8 tests）。
  - 前端：`frontend\\npm.cmd run build` 通过（可启动 dev server 于 5173）。
- 下一步：更新 `docs/部署说明.md` 增加前端启动步骤；用浏览器实际验证 RBAC 页面跳转与 CSV 下载；随后进入 ATT-001 二维码签到 v1。

### 2026-02-24 03:23:18
- 目标：落地 ADMIN-001 管理员端完整功能（数据库/后端/前端/测试/证据链），满足“用户/院系班级/实验室/设备/学期/日志/导出”可演示。
- 执行：
  - 数据库：新增 Flyway `V5__admin_seed_data.sql`（院系/班级/实验室/设备/学期最小种子数据），确保 H2 与 MySQL 都可迁移至 v5。
  - 后端：新增 `/api/admin/**` 管理员接口（用户管理、角色只读、院系班级、实验室、设备、学期、审计日志、CSV 导出），并对所有写操作写入 `audit_log`；所有 CSV 导出统一加 UTF-8 BOM + `Content-Disposition`。
  - 前端：扩展 `frontend/` 的 `/admin` 为完整管理后台（Tabs：用户/院系/班级/实验室/设备/学期/操作日志/备份说明/RBAC 自检），实现导入/导出与 CRUD 操作。
  - 测试：新增 `AdminIntegrationTests` 覆盖管理员 CRUD + 越权 403 + 导出 BOM + 审计记录存在；回归 `mvn test` 全通过。
- 结果：
  - 后端：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过（包含新 AdminIntegrationTests）。
  - 前端：`frontend\\npm.cmd run build` 通过。
  - 文档：`docs/接口与数据库.md` 已补齐管理员端表与接口清单；`docs/CODEX_执行说明.md` 追加管理员端审计与 CSV 规则。
- 下一步：更新“进度报告”脚本与材料生成脚本（用例图/数据库说明）以覆盖管理员端，并在 `01_证据截图` 生成管理员导出 CSV 证据（Excel 打开中文不乱码）。

### 2026-02-24 03:33:27
- 目标：修复“新增班级后，编辑用户的班级下拉框不实时更新，必须刷新网页”的体验问题。
- 执行：在管理员端 `用户管理` 面板中，打开“新建用户/编辑用户”弹窗前自动重新拉取最新的角色/院系/班级列表；同时在下拉框显示加载状态，避免旧缓存导致异常。
- 结果：新增班级后无需刷新网页，回到用户管理点击编辑即可看到新班级选项；前端构建通过（`npm run build`）。
- 下一步：若还出现“内部错误”，提供后端控制台报错日志（含 traceId），我会按 traceId 定位具体接口与参数并修复。

### 2026-02-24 03:40:20
- 目标：把本轮实现同步到 GitHub，满足“继续下一任务前先推送”的规则。
- 执行：`git add -A`、提交并推送到 `origin/main`。
- 结果：已推送提交 `ea19baf98feb1c0ff137bd004c8410b9248a213d`（ADMIN-001 + 前端实时刷新修复一起入库）。
- 下一步：后续每次你说“继续下一任务/开始下一步”且未说明崩溃，我会先 push 再进入下一任务。

### 2026-02-24 13:47:36
- 目标：修复“老师端看不到学生提交文字”的问题，并补齐“学生上传附件、老师下载/预览附件、下载报告文本”的能力。
- 执行：
  - 后端：新增附件存储配置 `app.storage.dir`（默认 `uploads`，测试用 `target/test-uploads`）；新增附件接口：
    - `POST /api/submissions/{submissionId}/attachments`（学生上传）
    - `GET /api/submissions/{submissionId}/attachments`（授权后查看列表）
    - `GET /api/attachments/{attachmentId}/download`（授权后下载附件）
    - `GET /api/submissions/{submissionId}/content/download`（下载 Markdown 文本，UTF-8 BOM）
  - 前端：教师端新增“查看报告”弹窗展示 `contentMd`，支持下载文本、查看/下载/预览（图片）附件；学生端新增“附件管理”弹窗支持上传/查看/下载/预览（图片）附件。
  - 测试：扩展 `AuthAndWorkflowIntegrationTests` 覆盖附件上传/权限访问/下载与文本下载 BOM；本机 `mvn test`、前端 `npm run build` 通过。
- 结果：老师端可直接看到报告文字并可下载；学生可上传附件，老师与学生均可下载附件，图片可在线预览。
- 下一步：若需要“老师端下载整份报告包（文字+附件打包）”，再追加 ZIP 导出接口与一键下载按钮。

### 2026-02-24 20:26:27
- 目标：补齐 SCHED-001（教师周课表）与 ATT-001（动态二维码签到 v1）可演示闭环；并补充管理员删除“仅学生”用户能力。
- 执行：
  - 数据库：新增 Flyway `V6__schedule_core.sql`、`V7__attendance_core.sql`、`V8__schedule_attendance_seed_data.sql`；本机 MySQL `lab_flow_report` 已迁移到 v8。
  - 后端：新增课表与签到模块接口（管理员维护节次/课表；教师周课表/名册；教师开启签到/动态 token/实时名单/补签/导出；学生扫码登录签到）。
  - 前端：教师端新增“课表/签到”页（周表格、动态二维码、实时名单、补签、导出）；学生端新增 `/m/checkin` 扫码签到页；管理员端增加节次/课表管理面板。
  - 管理端：用户管理新增“删除（仅学生）”按钮与 `DELETE /api/admin/users/{id}`（若学生已有提交/签到记录则拒绝删除并提示禁用）。
  - 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过。
  - Git：提交并推送到 `origin/main`。
- 结果：
  - GitHub 已推送提交：
    - `a7f35a0`（SCHED-001/ATT-001：周课表 + 动态二维码签到）
    - `4dc95f1`（ADMIN：删除仅学生用户）
- 下一步：按你给的“第1-12节标准节次时间表”补齐 `time_slot` 种子数据，使周课表显示更贴近真实作息。

### 2026-02-26 00:34:05
- 目标：实现 FE-LGN-001 + LAN-001：登录回车提交；全站桌面/手机模式切换（可记住）；同 WiFi 手机可访问前端并正确连后端。
- 执行：
  - 前端：登录页支持 Enter 提交；新增“界面模式”切换（自动/桌面/手机）并持久化；新增“记住账号密码”（下次自动填充）；全站头部加入模式切换；API 默认基址改为 `http(s)://{当前主机}:8080`，解决手机访问时 `localhost` 指向错误问题；`npm run dev` 默认监听 `0.0.0.0:5173`，新增 `npm run dev:local` 只本机。
  - 后端：CORS 改为 `allowedOriginPatterns`，默认放行本机与常见局域网网段的 `5173` 来源，并支持用环境变量 `CORS_ALLOWED_ORIGIN_PATTERNS` 覆盖。
  - 测试：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：手机同 WiFi 可用 `http://<PC局域网IP>:5173` 打开前端并正常调用后端（不再因 CORS/localhost 导致失败）。
- 下一步：若要外网访问，单独规划“部署或内网穿透（cloudflared/ngrok）+ HTTPS”。

### 2026-02-26 01:00:29
- 目标：实现 MOB-UX-001：修复手机输入法触发页面缩放；学生端手机版“任务列表优先”；教师端手机版隐藏侧栏并保证课表可横向滚动。
- 执行：
  - 全局移动端：`ui=mobile` 下输入框/文本框字号强制 >= 16px（避免 iOS 自动放大）；页面高度补 `100dvh` 减少键盘弹出导致的跳动。
  - 模式切换：手机端把模式切换控件改为下拉按钮，避免 header 挤爆。
  - 学生端：手机模式改为“先列表后详情”，首页只显示实验任务列表；点任务进入任务详情页（提交报告 + 我的提交折叠面板 + 返回列表）。
  - 教师端：手机模式隐藏左侧任务栏，改为顶部选择任务；课表区域保持现有横向滚动。
  - 测试：`frontend\\npm.cmd run build` 通过。
- 结果：手机输入时不再需要手动缩放；学生端手机页面不会被侧栏挤占；教师端手机可完成核心操作。
- 下一步：按你真实作息补齐 1-12 节标准节次并优化课表显示。

### 2026-02-26 01:29:43
- 目标：修复签到二维码/链接仍指向 `localhost` 导致手机扫码打不开的问题；并把“上传附件入口”放到学生端提交报告区域（不必先去“我的提交”找附件按钮）。
- 执行：
  - 后端：新增 `GET /api/ping/lan-ips` 返回本机局域网 IPv4 列表与推荐 IP。
  - 前端（教师端）：课表/签到弹窗里 `mobileBase` 新增本地持久化与“使用推荐IP”按钮；当基址是 `localhost` 时给出明确提示，并在打开班级管理页时自动拉取局域网 IP（若用户未配置过基址则自动填充为推荐 IP）。
  - 前端（学生端）：在“提交报告”区域新增“附件（上传到最新一次提交）”上传入口与“管理/预览”按钮，复用原有附件能力。
  - 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过。
- 结果：老师端打开签到后默认会给出可被手机访问的签到链接（不再默认 `localhost`）；学生上传附件不需要切到“我的提交”。
- 下一步：如果你希望“扫码后不经过前端 5173 也能签到”（只扫 `8080`），再加一个后端 H5 页面或把前端打包到后端静态资源。

### 2026-02-26 01:46:27
- 目标：签到导出 CSV 同时包含“已签到/未签到”全名单；并补一个隐藏的管理员端 bug（创建用户时 departmentId 为空会 500）。
- 执行：
  - 后端：签到导出 `GET /api/attendance/sessions/{id}/export` 改为“班级名册全量导出”，每个学生一行，新增列 `status`（CHECKED_IN / NOT_CHECKED_IN）。
  - 后端：修复管理员 `POST /api/admin/users` 审计写入使用 `Map.of(...)` 不能包含 null 的问题（departmentId/classId 允许为空），避免 500。
- 测试：更新 `ScheduleAttendanceIntegrationTests` 覆盖未签到学生也会出现在 CSV；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：老师导出的签到表能直接看出谁没签到；管理员创建学生账号（只填 classId 不填 departmentId）不再 500。

### 2026-02-26 01:55:48
- 目标：二维码签到支持“动态刷新秒数可配置”与“静态二维码模式”（动态仍为默认）。
- 执行：
  - 前端（教师端）：在“班级管理 / 签到”工具栏新增二维码模式选择（动态/静态）与动态刷新秒数输入；静态模式不自动刷新二维码，仍可手动点“刷新二维码”；动态模式按设置秒数自动刷新。
  - 持久化：二维码模式与秒数写入 localStorage（下次进入沿用）。
  - UI：二维码标题文案随模式/秒数变化。
  - 测试：`frontend\\npm.cmd run build` 通过。
- 结果：老师可按现场网络情况选择动态刷新频率或静态二维码；默认仍为动态 5 秒刷新。

### 2026-02-26 02:06:23
- 目标：让“静态二维码”真正可用，不再受动态 token TTL=6 秒限制；并解释“动态刷新间隔为什么不能大于 TTL”。
- 执行：
  - 数据库：新增 Flyway `V9__attendance_static_qr.sql`，给 `attendance_session` 增加 `static_code`（静态签到码）并加唯一索引。
  - 后端：新增接口
    - `GET /api/attendance/sessions/{sessionId}/static-code`（教师/管理员获取或生成静态码）
    - `POST /api/attendance/checkin/static`（学生用静态码签到）
    - 动态 token 仍走原来的 `/token` + `/checkin`，TTL=6 秒用于“旧码快速失效”。
  - 前端：教师端“静态模式”改为使用后端静态码生成二维码，二维码在签到未结束前一直有效；学生端 `/m/checkin` 支持 `?c=静态码`（静态）或 `?t=token`（动态）。
- 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过（覆盖静态+动态两种签到）；`frontend\\npm.cmd run build` 通过。
- 结果：需要“长一点”的场景直接切换静态二维码即可；动态二维码继续保持短 TTL 的安全性质。

### 2026-02-26 02:23:51
- 目标：实现 ATT-TTL-001：动态二维码 TTL 支持“每场次可配置并可延长（最大 60 秒）”，并让刷新间隔能配到更长。
- 执行：
  - 数据库：新增 Flyway `V10__attendance_dynamic_ttl.sql`，为 `attendance_session` 增加 `token_ttl_seconds`（默认 6）。
  - 后端：
    - `POST /api/attendance/sessions` 支持可选 `tokenTtlSeconds`（创建新场次时写入 session）。
    - 新增 `PUT /api/attendance/sessions/{sessionId}/token-ttl`（仅 OPEN 场次，3-60 秒）。
    - `GET /api/attendance/sessions/{sessionId}/token` 返回的 `ttlSeconds` 改为该场次的 TTL。
    - 动态 token 校验：`AttendanceTokenService` 只校验签名；过期判断改为在 `AttendanceService.checkInByToken` 按 session 的 TTL 判定。
  - 前端（教师端）：动态模式新增 TTL 秒输入与“应用 TTL”按钮；并持久化到 localStorage；开启新场次时默认带上该 TTL。
  - 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过（覆盖 TTL=30/60 与 TTL=3 过期行为）；`frontend\\npm.cmd run build` 通过。
- 结果：老师可把动态码 TTL 调到 30/60 秒，从而把“刷新间隔”调大；同时仍保留静态二维码作为更稳的长效方案。

---
- 时间：2026-02-26 02:43:30
- 目标：教师端签到仅保留“刷新秒数”设置，后端 TTL 自动跟随（TTL=refresh+1，封顶60）
- 执行：
  - 前端：移除 TTL 输入/应用按钮/本地存储；新增自动 TTL 同步（debounce），开启签到时带 tokenTtlSeconds；动态模式切换时自动同步
  - 验证：frontend 构建；后端 mvn test
- 结果：
  - build/test 通过；教师端不再需要手动配置 TTL
- 下一步：
  - 手工联调：教师开签到后改刷新秒数（10/60）观察 token ttlSeconds 变化；确认静态码不受影响

---
- 时间：2026-03-03 18:20:58
- 目标：实现 PLAG-UI-001：教师在“查看报告/评分”弹窗直接看到查重率、最高相似对象、相似句子高亮，并联动附件证据（代码/截图）
- 执行：
  - 数据库：新增 Flyway V11/V12（plag_task_run / plag_artifact_fp / plag_submission_best_match）
  - 后端：新增查重运行与摘要接口
    - POST /api/teacher/tasks/{taskId}/plagiarism/run
    - GET /api/teacher/submissions/{submissionId}/plagiarism-summary
    - 文本：SimHash64；图片：dHash64；xlsx/xls 等类型跳过并记录原因
    - 生成相似句子 fragments（Jaccard）用于高亮
  - 前端：教师端“查看报告/批阅评分”弹窗加入 PlagiarismSummaryPanel（高亮视图 + 附件下载/图片对比预览 + 一键运行查重）
  - 测试：lab-flow-report-system mvn test 通过；frontend npm build 通过
- 结果：
  - 教师无需跳转页面即可在弹窗看到查重摘要与高亮证据
- 下一步：
  - 手工演示：创建两份相似报告+附件，运行查重，确认弹窗高亮与图片对比可用；确认 MySQL 实库 Flyway 能迁移到 v12

---
- 时间：2026-03-03 22:20:00
- 目标：实现 SUB+ATT+PLAG-003：一次性提交（正文+附件）+ 禁止无新增内容提交 + 空正文确认 + 学生查看正文 + 代码附件预览 + 图片查重可见性增强
- 执行：
  - 数据库：新增 Flyway `V13__submission_attachment_hash.sql`
    - `report_submission.content_sha256`
    - `report_attachment.file_sha256` + 索引
  - 后端：
    - 新增 `POST /api/tasks/{taskId}/submissions/multipart`（contentMd 可空；支持 files[]；空正文需 confirmEmptyContent）
    - 旧 JSON 提交 `POST /api/tasks/{taskId}/submissions` 增加“正文无变化禁止重复提交”
    - 附件保存时计算 SHA-256 并写库；对比上一版本附件 SHA-256 判定“无新增附件”
    - 查重摘要新增“图片/文本附件参与与跳过统计”，并在前端展示，便于定位“为什么没出现图片相似度”
    - 动态码过期判定从 `>` 调整为 `>=`（TTL=3 的测试能稳定过期）
  - 前端：
    - 学生端：提交区支持一次性选择多附件；新增“查看内容/下载正文”；提交时增加确认框（空正文/无附件提醒）
    - 学生端/教师端：附件预览支持图片 + 小型文本/代码（>200KB 提示下载）
    - 教师端：查重摘要面板展示图片参与/跳过统计与提示
  - 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过
- 结果：
  - 学生不再需要“先提交再传附件”；禁止无新增内容刷版本；老师能一眼看到图片是否参与查重以及跳过原因

---
- 时间：2026-03-06 09:40:00
- 目标：整理并提交上一轮已验证通过的课表、查重、移动端与管理端增强功能
- 执行：
  - 学生端：新增“我的课表”入口，桌面端支持周表查看，手机端支持按日程纵向查看
  - 教师端：提交列表按学生聚合展示；查看报告/批阅弹窗补齐历史版本查重信息与宽度优化
  - 课表数据：补充演示课程、教室与多班级周课表数据，便于联调排课与签到
  - 管理端：支持编辑用户 `username` 与 `displayName`
  - 查重：补齐“历史版本风险”展示，老师查看最新版本时可同时看到该学生过往版本的最高风险
- 结果：
  - 上述功能均已通过前端构建/后端测试与人工验证，进入版本提交阶段

---
- 时间：2026-03-06 09:45:00
- 目标：启动 ATT-HIST-001 + CLASS-GRADE-001
- 执行：
  - 开始实现教师端“历史签到中心”
  - 开始实现管理员班级结构化字段 `grade + name + displayName`
- 结果：
  - 进入本轮功能开发
---
- 时间：2026-03-08 23:55:00
- 目标：完成 ATT-HIST-001 + CLASS-GRADE-001 并提交稳定版本
- 执行：
  - 教师端：新增“历史签到”中心，支持按年级/班级/课室/时间/状态筛选历史签到，查看整班已到/未到详情，并可重新导出签到 CSV
  - 管理端：班级结构从单字段升级为 `grade + name + displayName`，补齐院系/年级/关键词查询、导出与全站显示兼容
  - 后端：新增教师历史签到查询接口与班级显示工具，数据库迁移升级到 `V16__org_class_grade.sql`
  - 测试：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过
- 结果：
  - 历史签到不再依赖现场弹窗留存；班级“级/班”结构化完成并进入版本提交阶段

---
- 时间：2026-03-08 23:56:00
- 目标：启动 FLOW-001（实验过程管理 + 完成登记确认 + 设备申请审批借还）
- 执行：
  - 开始实现学生实验步骤记录、教师过程监督与完成登记确认
  - 开始实现按任务配置设备、学生申请、教师审批借出归还与导出记录
- 结果：
  - 进入本轮功能开发

---
- 时间：2026-03-09 18:35:00
- 目标：确认当前版本为稳定版，并标注为“首次出现较大问题后完成收口修复”的版本
- 执行：
  - 完成 `FLOW-001`、整学年课表测试数据、课表加载修复、历史签到权限/名册修正、稳定性收口 `STAB-001`
  - 修复 `V24` Flyway checksum 冲突，新增本地修复脚本 `lab-flow-report-system/scripts/repair-flyway-local.ps1`
  - 完成稳定性核查：
    - 课表：教师端/学生端可正常读取，支持整学年与单双周测试
    - 历史签到：结束后可在历史中心查询，详情不再把教师算入名册
    - 实验流程：步骤记录、完成登记、教师确认、设备申请审批借还可用
    - 设备库存：关键路径增加锁保护，避免并发超借
  - 验证：
    - `lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过
    - `frontend\\npm.cmd run build` 通过
    - 本地 MySQL 执行 Flyway repair 后，后端恢复可启动
- 结果：
  - 当前版本确认为可上传版本
  - 标注说明：这是项目开发过程中“第一次出现较大问题并完成集中修复后”的稳定版基线，后续若继续扩展功能，可据此版本对比回归
---
- 时间：2026-03-09 20:00:00
- 目标：完成 TASK-ATT-001 与任务管理细化能力并提交当前可用版本
- 执行：
  - 任务附件：教师端新建任务支持多附件上传，任务创建后可继续追加上传/删除；学生端在任务要求区可查看并下载任务资料
  - 后端：新增 `task_attachment` 表与任务附件接口，任务详情返回附件列表，附件独立存储到 `uploads/task-attachments/{taskId}`
  - 任务管理：新增“删除任务”能力，仅允许删除没有提交、实验步骤、完成登记、设备申请和查重记录的空任务，便于清理 RBAC 自检任务
  - 任务管理：新增“修改任务名”能力，教师端可直接编辑任务标题并同步刷新列表与详情
  - 验证：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过
- 结果：
  - 任务布置资料链路完整打通；空测试任务可安全清理；任务标题可直接维护
---
- 时间：2026-03-09 20:35:00
- 目标：按当前确认结果上传教师多班级绑定版本，作为新的远端开发基线
- 执行：
  - 完成 `USER-CLASS-001`：仅教师支持多班级绑定，学生继续保持单班级
  - 新增教师-班级关联表 `sys_user_class`，新增迁移 `V26__teacher_class_binding.sql`
  - 管理端用户创建/编辑/查询/导出支持 `classIds`，教师班级在列表中按展示名聚合显示
  - 管理端用户页编辑弹窗中，教师角色的“班级”改为多选；学生角色仍保持单选
  - 后端增加角色语义约束，避免教师/学生双角色导致班级语义冲突
- 结果：
  - 当前工作区版本按日常规则进入 GitHub 主分支，作为后续继续实施新计划的开发基线
---
- 时间：2026-03-10 00:20:00
- 目标：完成 TASK-QC-DEL-001 / TASK-QC-DEL-002，并将自检任务清理能力纳入新的开发基线
- 执行：
  - 将 `DELETE /api/tasks/{taskId}` 升级为强制物理删除，移除“有业务记录不能删”的限制
  - 删除链路覆盖任务附件、报告提交/附件/批阅、实验过程记录/附件、完成登记、设备申请、查重结果、发布班级绑定等关联数据，并同步删除对应文件
  - 新增本地脚本 `lab-flow-report-system/scripts/cleanup-qc-tasks.ps1`，仅用于清理 `RBAC自检任务` / `E2E演示任务`
  - 补充后端集成测试，验证教师/管理员删除带业务记录任务、非发布教师 403、删除后学生端任务列表不再返回该任务
  - 实际对当前 MySQL 执行清理，删除历史遗留 `RBAC自检任务` 共 17 条；保留 `test`、数字标题、`????` 等手动处理任务
  - 验证：`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过；`frontend\\npm.cmd run build` 通过
- 结果：
  - 具备强制物理删除测试任务的后端能力
  - 历史 `RBAC自检任务` 已从真实库清理，不再继续污染学生端任务列表
  - 当前代码作为继续实施外网访问方案前的最新开发基线
---
- 时间：2026-03-10 01:00:00
- 目标：完成登录页提示清理与 Quick Tunnel 临时外网访问修复并上传当前版本
- 执行：
  - 登录页：移除用户名/密码输入框中的演示账号占位提示，删除底部演示账号说明区块
  - 前端：默认 API 基址改为同源 `/api`，Vite dev server 新增 `/api`、`/actuator` 代理，并允许 `.trycloudflare.com` Host
  - 后端：CORS 默认允许 `https://*.trycloudflare.com`，兼容 Cloudflare Quick Tunnel 的公网登录请求
  - 验证：公网域名下带 `Origin=https://*.trycloudflare.com` 的 `/api/auth/login` 返回标准 JSON；`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过
- 结果：
  - 登录页不再暴露演示账号密码文案
  - 临时 Quick Tunnel 外网访问链路具备同源代理与 CORS 支持，可作为后续公网演示基线
- 时间：2026-03-11 02:05:00
- 目标：上传当前工作区全部改动，统一教师班级来源并修复学生编辑误判、班级管理收口问题
- 执行：
  - 教师端新建任务默认班级来源改为管理员绑定班级，`scope=mine` 读取 `sys_user_class`，保留 `scope=schedule` 作为课表班级语义
  - 教师端新建任务弹窗文案由“我的课表班级”调整为“我的绑定班级”，说明文字同步改为管理员绑定语义
  - 修复管理端编辑学生时误触“仅教师账号支持多班级绑定”的问题：学生保存仅走单班级，后端对空/单元素兼容 `classIds` 做忽略处理
  - 新增迁移 `V27__org_class_unique_by_grade.sql`，统一班级唯一性为 `department_id + grade + name`
  - 新增迁移 `V28__org_class_data_cleanup.sql`，收口当前基础班级数据语义，使 2022/2023 软件工程班与化学班名称、年级展示一致
  - 管理端班级页文案清理：移除备份与导出页顶部说明，去掉“给老师看的口径”字样
  - 补充后端测试，覆盖学生编辑兼容 classIds、教师绑定班级来源与班级唯一性相关场景
- 结果：
  - 教师班级来源按“管理员绑定”和“课表班级”两条语义分离收口
  - 学生编辑用户不再因兼容字段误报多班级绑定错误
  - 班级基础数据与唯一性规则进入同一套设计口径，作为后续继续开发的最新基线
---
---
- 时间：2026-04-04 22:00:00
- 目标：完成 ELECTIVE-COURSE-001，新增实验课程开放选课与任务按实验课程发放能力
- 执行：
  - 新增实验课程领域模型与迁移 `V29`~`V33`，包含实验课程主表、可选场次、开放班级/指定学生、选课记录，以及 `exp_task.experiment_course_id`
  - 新增教师端实验课程接口，支持创建/编辑实验课程、配置 1 个或多个场次、设置开放状态与截止选课时间、查看选课名单
  - 新增学生端实验课程接口，支持查询可选课程、按场次先到先得选课、查看我的已选课程
  - 学生任务可见性扩展为并集：命中任务班级范围或已选上任务绑定的实验课程，任一命中即可见
  - 学生课表接口合并实验课程场次返回，新增 `sourceType=EXPERIMENT_COURSE` 以区分普通班级课表与实验课程
  - 前端教师端新增“实验课程”页与任务弹窗“关联实验课程（可选）”，学生端新增“实验课程”标签页与选课界面
  - 选课写入增加事务与容量校验，保证同一课程单学生仅能选择一个场次并避免超卖
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -Dtest=ExperimentCourseIntegrationTests test` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过
- 结果：
  - 系统新增独立“实验课程开放选课”业务线，不改写现有 `course_schedule` 语义
  - 教师可按开放班级和指定学生并集开放实验课程，学生可按场次完成选课
  - 任务支持绑定实验课程，学生课表可展示已选实验课程场次，作为后续公网演示与论文答辩的新基线
- 时间：2026-04-05 19:05:00
- 目标：收口实验课程重复规则、师生课表/签到联动与认证异常提示，并上传当前版本
- 执行：
  - 实验课程：重复规则移除“双周”，保留“每周/单周”，其中“单周”改为从首次上课开始每 14 天生成一次，不再按学期奇偶周判定
  - 实验课程：前端教师端场次配置、实例预览、学生端场次摘要同步改为新语义，删除“首次上课日期与单双周设置不匹配”提示
  - 新增迁移 `V37__experiment_course_remove_even_week.sql`，将历史 `EVEN_WEEK` 数据收口到新语义；新增启动同步逻辑，仅对无签到历史引用的重复场次重建实例，避免外键冲突
  - 师生课表/签到：实验课程实例并入教师课表与学生课表，签到绑定到具体实验课程实例，历史、详情、导出和补签统一按场次组报名学生口径统计
  - 认证异常：前端统一把非 JSON 代理失败/后端离线响应转换为明确错误提示，后端认证失败链补测试验证 401/403 均返回标准 JSON
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -Dtest=ExperimentCourseIntegrationTests test` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过
- 结果：
  - 实验课程重复规则已切换到以首次课为起点的每周/隔周模型，界面和后端生成逻辑一致
  - 实验课程选课、任务、课表、签到、历史与导出形成闭环，教师端旧班级课表/签到入口已隐藏为实验课程链路
  - 登录态异常与后端离线场景的前端提示更可诊断，可作为当前演示与继续开发基线
### 2026-04-05 23:16:00
- 目标：收口实验课程手动报名管理（V38）与教师直接登记完成/移动端适配（V39），并完成上传前核验。
- 执行：
  - 新增实验课程报名管理：教师手动加入学生到具体场次、踢出、禁选名单、解除禁选。
  - 新增完成登记来源字段 `completion_source`，支持教师直接登记完成。
  - 补齐实验课程相关教师端手机版 UI 与学生端状态展示。
  - 依次执行 `frontend\\npm.cmd run build`、`lab-flow-report-system\\.\\mvnw.cmd -Dtest=ExperimentCourseIntegrationTests test`、`lab-flow-report-system\\.\\mvnw.cmd -Dtest=ExperimentFlowIntegrationTests test`、`lab-flow-report-system\\.\\mvnw.cmd -U clean test`，全部通过。
- 结果：本地工作区已达到可上传状态，准备提交并推送到 `origin/main`。
- 下一步：提交 `V38 + V39` 并推送；后续按已上传版本继续验收。
### 2026-04-05 23:45:00
- 目标：完成 LAB-ROOM-OPENING-001，升级实验室信息为结构化开放时段，并开放教师端实验室管理。
- 执行：
  - 新增迁移 `V40__lab_room_open_slots.sql`，建立 `lab_room_open_slot` 结构化开放时段表。
  - 新增 `LabRoomManagementService`、`TeacherLabRoomController`、开放时段 DTO/Entity/Mapper，统一管理员端与教师端共用同一套全局实验室库。
  - 管理员端实验室面板升级为可维护多条按周开放时段规则；教师端新增“实验室管理”tab，手机版使用卡片流与纵向编辑布局。
  - 实验课程元数据与管理员排课实验室下拉补充开放时间摘要展示，便于老师选场地时直接查看常规开放规则。
  - 新增 `LabRoomManagementIntegrationTests`，覆盖教师/管理员共享库、开放时段重叠校验、管理员更新/删除、教师实验课程元数据可见开放时间摘要。
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -Dtest=LabRoomManagementIntegrationTests test` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：
  - 实验室管理从纯文本开放时间升级为结构化周规则，教师与管理员已可共同维护同一套实验室信息。
  - 实验课程与管理员排课页面已联动显示开放时间摘要，为后续开放时段冲突校验保留了结构基础。
- 下一步：提交并推送本批实验室开放时段改动。
### 2026-04-06 11:05:00
- 目标：完成教师端与管理员端统计面板第一版，并提交上传当前版本。
- 执行：
  - 新增教师端 `统计报表` tab 与管理员端 `统计面板` tab，支持当前学期默认筛选、教师/班级下钻、图表展示和分区块 CSV 导出。
  - 新增后端统计聚合服务与教师/管理员统计接口，汇总任务、提交、批阅、完成登记、签到、实验课程、设备申请等核心指标。
  - 前端引入 `echarts`，新增共享 `StatsChart` 组件以及教师端/管理员端统计面板组件，补齐桌面端与手机端基础可用布局。
  - 新增 `StatisticsDashboardIntegrationTests`，覆盖教师端和管理员端统计面板与 CSV 导出接口。
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -Dtest=StatisticsDashboardIntegrationTests test` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：
  - 教师端和管理员端已具备可用的统计总览、趋势图与分区块 CSV 导出能力。
  - 当前版本达到上传基线，后续将在此基础上继续升级为完整 Excel 多工作表明细导出。
- 下一步：提交并推送统计面板改动；随后开始全系统 Excel 明细导出改造。
### 2026-04-06 14:25:00
- 目标：完成 EXPORT-DETAIL-XLSX-001，新增全系统 Excel 多工作表明细导出，并保持现有 CSV 导出与管理员用户 CSV 批量导入不变。
- 执行：
  - 新增共享 `ExcelExportService` 与 `ExportResponseHelper`，引入 `Apache POI` 统一生成 `.xlsx` 附件响应。
  - 为教师/管理员统计面板补齐 Excel 导出接口与前端按钮，导出内容覆盖筛选条件、汇总与多张明细工作表。
  - 为任务成绩、签到记录、实验过程/设备申请、管理员资源导出（用户、院系、班级、实验室、设备、学期、审计日志）新增 Excel 版本，同时保留原 CSV 导出链路。
  - 管理员端备份页同步增加 Excel 下载入口；用户管理里的 CSV 批量导入入口、模板、解析逻辑与后端 `AdminUserService#importUsers` 保持不变。
  - 修复新增导出基础设施引入的 Java 泛型与班级展示编译问题，完成前后端全量回归。
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：
  - 现有所有 CSV 导出仍可使用，同时新增可核对完整业务明细的 Excel 多工作表导出。
  - 管理员用户管理的 CSV 批量导入功能未被本轮改动影响。
- 下一步：根据验收效果决定是否后续收口移除部分 CSV 导出按钮。
### 2026-04-06 16:43:46
- 目标：完成 TASK-DISCUSSION-001 与 TASK-DISCUSSION-REFINE-001，补齐任务讨论区、教师端聚合问答、未读提醒，以及讨论区刷新与独立页面体验收口。
- 执行：
  - 新增任务讨论区后端模型与接口，包含 `task_discussion_thread`、`task_discussion_message`、`task_discussion_read_state` 三张表及 `V41__task_discussion_core.sql` 迁移。
  - 新增学生端任务讨论接口与教师端聚合问答接口，支持 `NORMAL` / `ASK_TEACHER` 线程、已读标记、未读汇总与教师聚合列表。
  - 新增前端共享 `TaskDiscussionPanel` 组件，学生端按任务进入独立讨论页，教师端新增 `讨论区` tab 与按任务聚合问答视图。
  - 修正学生任务列表讨论未读 badge 位置，改为独立右侧数字角标；讨论区支持手动刷新、进入页面自动刷新一次。
  - 保留讨论自动刷新，但将其改为静默刷新并在刷新前后记录与恢复页面滚动位置，避免阅读时自动回到顶部；未读红点继续轻量轮询。
  - 验证：`frontend\\npm.cmd run build` 通过；`lab-flow-report-system\\.\\mvnw.cmd -Dtest=TaskDiscussionIntegrationTests test` 通过；`lab-flow-report-system\\.\\mvnw.cmd -U clean test` 通过。
- 结果：
  - 学生端已具备按任务讨论区、教师问答与普通讨论分区、任务级未读提醒与独立讨论页。
  - 教师端已具备讨论区聚合问答、未读红点与进入任务讨论的联动能力，讨论自动刷新不再打断阅读位置。
- 下一步：提交并推送任务讨论区相关改动；后续如需进一步优化，可考虑只对当前展开线程做增量更新。

## 2026-04-06 AUTH-COURSE-ATTENDANCE-FIX-001
- 修复提交访问越权：教师批阅、查看批阅结果、下载附件与正文统一按 submissionId 反查 taskId，再校验任务发布教师或管理员权限。
- 修复实验课程任务可见性：任务绑定 experiment_course_id 时改为课程优先，仅当前 ENROLLED 学生可见，不再允许班级目标兜底。
- 完成实验课程签到历史名单快照：启用 attendance_session_roster，开签到时冻结名单，历史详情、CSV、Excel、统计优先读取快照，老会话兼容回退旧逻辑。
- 修复已有签到历史的场次编辑：保留被签到会话引用的历史实例，仅重建未来未引用实例；删除有历史签到的场次会返回业务错误。
- 新增并通过集成测试：提交越权访问、实验课程签到快照、历史场次未来重建与删除保护。
- 验证通过：frontend\\npm.cmd run build、lab-flow-report-system\\.\\mvnw.cmd -Dtest=AuthAndWorkflowIntegrationTests,ExperimentCourseIntegrationTests test、lab-flow-report-system\\.\\mvnw.cmd -U clean test。

## 2026-04-06 EXPERIMENT-COURSE-SEMESTER-MANAGE-001
- 在教师端实验课程新建/编辑弹窗中新增全局学期管理入口，可直接新建学期、编辑当前学期。
- 新增教师侧学期管理接口：`GET /api/teacher/semesters`、`POST /api/teacher/semesters`、`PUT /api/teacher/semesters/{id}`，复用管理员学期服务与同一套全局学期库。
- 学期创建/更新响应新增影响摘要：`affectedCourseCount`、`affectedInstanceCount`、`hasOutOfRangeCourses`、`warningMessage`，允许保存但对越界实验课程/课次给出明确警告，不自动调整历史课程与实例。
- 管理员端学期编辑同步复用相同影响检查与警告展示逻辑，保证教师端和管理员端口径一致。
- 将学期开始日期、结束日期改为必填，避免创建无法用于实验课程重复课次生成的无效学期。
- 新增并通过集成测试：教师可创建/编辑学期并收到越界警告、开始/结束日期必填校验生效。
- 验证通过：`frontend\\npm.cmd run build`、`lab-flow-report-system\\.\\mvnw.cmd -q -Dtest=ExperimentCourseIntegrationTests test`、`lab-flow-report-system\\.\\mvnw.cmd -U clean test`。

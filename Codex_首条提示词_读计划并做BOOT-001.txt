把下面内容整段粘贴到 Codex 的第一个任务里即可（建议：让 Codex 先产出计划与第1个PR）：

任务：读取本仓库并提出执行计划，然后完成 BOOT-001（工程底座）

要求：
1) 先阅读并总结：PLANS.md、log记录/决策日志.md、Codex_第1批任务单_启动包.md
2) 输出一个简短的执行计划（按里程碑拆分，每个里程碑给出验收标准），并把计划更新回 PLANS.md（只改计划与状态区，不改已定技术栈）
3) 然后实现并提交一个 PR：BOOT-001 工程底座与通用组件
   - 统一返回体、全局异常、traceId 日志、参数校验统一返回、Actuator health、Swagger 可访问、示例接口 /api/ping
4) PR 必须可本地验证：提供 Windows PowerShell 验证命令（例如 .\mvnw.cmd -U clean test）
5) 约束：保持工程化代码与注释；不要写对话痕迹或工具宣传语；不要引入 Redis；不要在未说明的情况下大改目录结构。

交付：
- 1 个 PR（仅 BOOT-001）
- PR 描述包含：改动文件清单、如何验证、已知风险（如无则写无）
